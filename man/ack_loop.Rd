% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roborev_blocklist.R
\name{ack_loop}
\alias{ack_loop}
\title{Acknowledge a loop entry to suppress blocking temporarily}
\usage{
ack_loop(
  content_hash,
  reason,
  until = NULL,
  by = Sys.info()[["user"]],
  path = path.expand("~/.claude/state/roborev_blocklist.json"),
  db_path = NULL
)
}
\arguments{
\item{content_hash}{Character scalar — hash to acknowledge.}

\item{reason}{Character scalar — human-readable reason.}

\item{until}{POSIXct or NULL.  If a timestamp, the block is suppressed only
until that time.}

\item{by}{Character scalar — who is acknowledging (default: current user).}

\item{path}{Blocklist JSON path.  Overridden by \code{ROBOREV_BLOCKLIST_PATH}.}

\item{db_path}{Optional path to \code{unified.duckdb} for audit sync.}
}
\value{
Invisibly returns the updated blocklist tibble.
}
\description{
Sets \code{ack_by}, \code{ack_at}, \code{ack_reason}, and optionally \code{ack_until} for the
entry with the given \code{content_hash}.  If \code{until} is \code{NULL}, the entry is
acknowledged indefinitely (the loop is considered resolved).
}
\details{
Also updates the same fields in \code{roborev_loops} in DuckDB if the database
is available.
}
