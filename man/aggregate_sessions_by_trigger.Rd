% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_sessions.R
\name{aggregate_sessions_by_trigger}
\alias{aggregate_sessions_by_trigger}
\title{Aggregate sessions.parquet rows by trigger provenance}
\usage{
aggregate_sessions_by_trigger(
  parquet_path = NULL,
  sessions_df = NULL,
  window_start = NULL,
  window_end = NULL
)
}
\arguments{
\item{parquet_path}{Path to \code{sessions.parquet}.  Defaults to
\code{inst/extdata/telemetry/v1/sessions.parquet} relative to the project root.
Ignored when \code{sessions_df} is supplied.}

\item{sessions_df}{Optional pre-loaded data frame of sessions (for tests or
in-memory use).  When supplied, \code{parquet_path} is ignored.}

\item{window_start}{Optional \code{Date} or \code{POSIXct}. Filter sessions with
\code{started_at >= window_start}.  \code{NULL} means no lower bound.}

\item{window_end}{Optional \code{Date} or \code{POSIXct}. Filter sessions with
\code{started_at <= window_end}.  \code{NULL} means no upper bound.}
}
\value{
A data frame with columns:
\describe{
\item{trigger}{Character. One of \code{"scheduled"}, \code{"interactive"}, \code{"unknown"}.}
\item{n_sessions}{Integer. Number of sessions with this trigger.}
\item{total_duration_min}{Numeric. Sum of \code{duration_min} (NA treated as 0).}
}
Rows are ordered by \code{trigger} alphabetically so the output is deterministic.
Returns a zero-row data frame with the correct column types if no sessions
match the window.
}
\description{
Reads \code{sessions.parquet} (or accepts a pre-loaded data frame) and returns a
tidy summary of session counts and total duration per trigger value.
}
\details{
\strong{Joinability note (Phase 2):} ccusage \code{type="session"} session IDs use the
format \code{"sanitized@{project}@h{12hex}"} (3-component, no timestamp), while
our \code{sessions.parquet} uses either raw UUIDs from staging hook events or the
4-component \code{"sanitized@{project}@{iso8601}@h{hash12}"} format.  These
schemes are incompatible; a clean key-join is NOT possible.  ccusage also
only provides \code{lastActivity} (date only, no time), ruling out time-overlap
joins at session granularity.

As a result, \strong{cost attribution} remains the Phase 1 block-window
heuristic (\code{classify_block_trigger()}).  This function provides the
complementary \emph{accurate} split: session count and duration by trigger for
rows where provenance is stamped.
}
