% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_costs.R
\name{append_costs_from_staging}
\alias{append_costs_from_staging}
\title{Append staged cost_emitted events into the v1 costs parquet}
\usage{
append_costs_from_staging(
  staging_dir = file.path(Sys.getenv("HOME"), ".claude", "logs", "llmtelemetry-staging"),
  parquet_path = here::here("inst", "extdata", "telemetry", "v1", "costs.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{staging_dir}{Path to staging directory.  Defaults to
\verb{~/.claude/logs/llmtelemetry-staging}.}

\item{parquet_path}{Path to costs.parquet.  Defaults to
\code{inst/extdata/telemetry/v1/costs.parquet}.}

\item{now}{Override the \code{valid_from} timestamp (mainly for tests).
Defaults to \code{Sys.time()}.}
}
\value{
Invisibly: integer count of NEW rows appended.
}
\description{
Reads JSONL files in the staging directory, filters to events with
\code{event_type == "cost_emitted"}, deduplicates by computed \code{cost_id}
against the existing parquet, and atomically appends new rows.
}
\details{
The synthetic key \code{cost_id} is \code{paste(canonical_project, date, source, sep="|")},
matching the key used by \code{rollup_costs()} during the JSON backfill.  A
staged event whose key already exists in the parquet is silently skipped.

Idempotent: running twice over the same staging files yields identical row
counts in the parquet.  \code{arrow} is not required; all Parquet I/O uses
DuckDB's native reader/writer.
}
