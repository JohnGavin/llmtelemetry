% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_git_commits.R
\name{append_git_commits_from_staging}
\alias{append_git_commits_from_staging}
\title{Append staged git_commit events into the v1 git_commits parquet}
\usage{
append_git_commits_from_staging(
  staging_dir = file.path(Sys.getenv("HOME"), ".claude", "logs", "llmtelemetry-staging"),
  parquet_path = here::here("inst", "extdata", "telemetry", "v1", "git_commits.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{staging_dir}{Path to staging directory.  Defaults to
\verb{~/.claude/logs/llmtelemetry-staging}.}

\item{parquet_path}{Path to git_commits.parquet.  Defaults to
\code{inst/extdata/telemetry/v1/git_commits.parquet}.}

\item{now}{Override the \code{valid_from} timestamp (mainly for tests).
Defaults to \code{Sys.time()}.}
}
\value{
Invisibly: integer count of NEW rows appended.
}
\description{
Reads JSONL files in the staging directory, filters to events with
\code{event_type == "git_commit"}, deduplicates by computed \code{commit_pk}
against the existing parquet, and atomically appends new rows.
}
\details{
The composite key \code{commit_pk} is \code{paste(canonical_project, hash, sep="|")},
matching the key used by \code{rollup_git_commits()} during the JSON backfill.
A staged event whose key already exists in the parquet is silently skipped.

Idempotent: running twice over the same staging files yields identical row
counts in the parquet.  \code{arrow} is not required; all Parquet I/O uses
DuckDB's native reader/writer.
}
