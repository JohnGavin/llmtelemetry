% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_sessions.R
\name{append_sessions_from_staging}
\alias{append_sessions_from_staging}
\title{Append staged session_stop events into the v1 sessions parquet}
\usage{
append_sessions_from_staging(
  staging_dir = file.path(Sys.getenv("HOME"), ".claude", "logs", "llmtelemetry-staging"),
  parquet_path = here::here("inst", "extdata", "telemetry", "v1", "sessions.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{staging_dir}{Path to staging directory.  Defaults to
\verb{~/.claude/logs/llmtelemetry-staging}.}

\item{parquet_path}{Path to sessions.parquet.  Defaults to
\code{inst/extdata/telemetry/v1/sessions.parquet}.}

\item{now}{Override the \code{valid_from} timestamp (mainly for tests).
Defaults to \code{Sys.time()}.}
}
\value{
Invisibly: integer count of NEW rows appended.
}
\description{
Reads JSONL files in the staging directory, filters to events with
\code{event_type == "session_stop"}, deduplicates by \code{session_id} against the
existing parquet, and atomically appends new rows.
}
\details{
Idempotent: running twice over the same staging files yields identical row
counts in the parquet.  \code{arrow} is not required; all Parquet I/O uses
DuckDB's native reader/writer.
}
