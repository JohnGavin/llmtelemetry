% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonicalize.R
\name{canonicalize_project}
\alias{canonicalize_project}
\title{Canonicalize raw project names recorded by hooks}
\usage{
canonicalize_project(x)
}
\arguments{
\item{x}{Character vector of raw project names (dash-form, slash-form, or
bare token).}
}
\value{
Character vector the same length as \code{x}.  Elements that represent
noise or agent worktrees are \code{NA_character_}; agent-tooling sessions are
\code{"agent-tooling"}; all others are the canonical single-token project name.
}
\description{
Maps raw project-path strings — as recorded by the Claude Code hook into
\code{unified.duckdb} — to their canonical single-token project name, or
\code{NA_character_} for pure noise (branch fragments, agent-worktree hashes,
container-directory names).  Real Claude usage sessions with no recoverable
parent project (e.g. \code{"roborev"}, \code{"sonnet"}, \code{"cc"}) are bucketed under
\code{"agent-tooling"}.
}
\details{
This is a vectorised wrapper around the internal
\code{.canonicalize_project_local_scalar()} helper.  It is suitable for use in
scripts that \code{source()} this file (e.g. \code{send_daily_email.R}) as well as
in package code.
}
\examples{
canonicalize_project(c("feat", "cc", "sonnet", "ab6f701adcaed79e9",
                        "llm", "footbet"))
# [1] NA             "agent-tooling" "agent-tooling" NA
# [5] "llm"          "footbet"

}
