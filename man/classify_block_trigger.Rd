% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccusage.R
\name{classify_block_trigger}
\alias{classify_block_trigger}
\title{Classify billing blocks as scheduled-automated or interactive (Phase 1)}
\usage{
classify_block_trigger(
  block_start_hour,
  block_end_hour,
  windows = default_automation_windows()
)
}
\arguments{
\item{block_start_hour}{Integer vector.  Hour of day (0–23) when the billing
block starts.  For standard ccusage blocks: one of 0, 5, 10, 15, 20.}

\item{block_end_hour}{Integer vector.  Hour of day (5–25) when the billing
block ends.  Use 25 for the 20:00–01:00 block that crosses midnight.}

\item{windows}{List of automation windows; defaults to
\code{\link[=default_automation_windows]{default_automation_windows()}}.  Each element must have \verb{$start} and
\verb{$end} numeric/integer fields.  Supply a custom list to override the
defaults in tests or different deployment environments.}
}
\value{
Character vector (\code{"scheduled"} or \code{"interactive"}), same length as
\code{block_start_hour}.
}
\description{
Phase 1 heuristic classifier for 5-hour ccusage billing blocks.  A block is
tagged \code{"scheduled"} \strong{only} when its entire window \verb{[block_start_hour, block_end_hour]} falls inside one of the known automation windows returned
by \code{\link[=default_automation_windows]{default_automation_windows()}}.  All other blocks are \code{"interactive"}.

\strong{Known approximation (Phase 1):} Billing blocks that contain \emph{both}
automated jobs and interactive work (e.g. the 05:00–10:00 block, which
holds both the overnight tail and the 09:00 roborev poller alongside morning
interactive sessions) are labelled \code{"interactive"} and the automated
portion's cost is misattributed.  This is acceptable for a labelled first
cut.  Accurate attribution requires per-session provenance tags (Phase 2,
issue #322).
}
\examples{
# Overnight block (00:00-05:00): falls inside [0, 7] -> scheduled
classify_block_trigger(0L, 5L)

# 05:00-10:00 block straddles 07:00 boundary -> interactive
classify_block_trigger(5L, 10L)

# Vectorised over all five daily blocks
classify_block_trigger(c(0L, 5L, 10L, 15L, 20L), c(5L, 10L, 15L, 20L, 25L))

}
\seealso{
\code{\link[=default_automation_windows]{default_automation_windows()}} for the window configuration.
}
