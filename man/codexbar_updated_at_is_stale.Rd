% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codexbar.R
\name{codexbar_updated_at_is_stale}
\alias{codexbar_updated_at_is_stale}
\title{Test whether a CodexBar \code{updated_at} timestamp is stale}
\usage{
codexbar_updated_at_is_stale(updated_at, now = Sys.time(), threshold_hours = 6)
}
\arguments{
\item{updated_at}{Character(1) or \code{NULL}.  ISO-8601 timestamp from a
CodexBar \code{updated_at} field.}

\item{now}{A \code{POSIXct} value representing the current time.  Defaults
to \code{Sys.time()}.  Pass an explicit value in tests for
deterministic results.}

\item{threshold_hours}{Numeric(1).  Staleness threshold in hours.  Default
is 6.}
}
\value{
\code{TRUE} if the gap between \code{updated_at} and \code{now}
exceeds \code{threshold_hours}; \code{FALSE} otherwise.
}
\description{
Returns \code{TRUE} when the timestamp is more than \code{threshold_hours}
hours before \code{now}.  Returns \code{FALSE} for \code{NA}, \code{NULL},
or an unparseable string — callers should treat \emph{unknown} freshness as
fresh rather than silently suppressing data.
}
