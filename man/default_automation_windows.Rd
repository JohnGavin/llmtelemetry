% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccusage.R
\name{default_automation_windows}
\alias{default_automation_windows}
\title{Default automation windows for Phase 1 block classification}
\usage{
default_automation_windows()
}
\value{
Named list of automation windows; each element has \verb{$start} and
\verb{$end} integer hour fields.
}
\description{
Returns the list of hour-range windows that are considered fully automated.
A 5-hour billing block is classified as \code{"scheduled"} only when its
\verb{[block_start_hour, block_end_hour]} interval is entirely contained within
one of these windows.

\strong{This is the single place to edit when adding or removing automation
schedules.} Each element is a named list with \code{start} (inclusive) and
\code{end} (inclusive) integer hours (0–24 clock, where 25 = 01:00 next day).

Current automation windows:
\itemize{
\item \strong{overnight} \verb{[0, 7]}: Overnight maintenance band. The 00:00–05:00
billing block falls entirely inside this window. Jobs include the
overnight digest, \code{config_pulse}, launchd nightly routines.
}

Note: the roborev business-hours poller (Mon–Fri 09:00/13:00/17:00) fires
inside blocks that also contain interactive work, so no purely-automated
window is defined for those slots. They are classified \code{"interactive"} by
Phase 1 and will be split accurately in Phase 2 (per-session provenance,
issue #322).
}
\seealso{
\code{\link[=classify_block_trigger]{classify_block_trigger()}} which consumes this list.
}
\keyword{internal}
