% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_loops.R
\name{detect_loops}
\alias{detect_loops}
\title{Detect recurring roborev finding loops}
\usage{
detect_loops(
  findings_df,
  agent_runs_df = NULL,
  jaccard_threshold = 0.6,
  window_days = 30L
)
}
\arguments{
\item{findings_df}{A tibble with columns matching the \code{roborev_findings}
schema (see issue #144).  Required: \code{reviewed_at} (POSIXct), \code{severity}
(chr/factor), \code{primary_file} (chr), \code{problem_text} (chr), \code{commit_sha}
(chr), \code{branch} (chr).}

\item{agent_runs_df}{Optional tibble of agent runs used to detect fix
commits.  Columns used: \code{agent_type} (chr), \code{commit_sha} (chr),
\code{started_at} (POSIXct), \code{total_cost_usd} (numeric).  If \code{NULL}, cycles
are still counted (any consecutive re-occurrence counts as a cycle), but
\code{estimated_wasted_usd} is \code{NA}.

NOTE: The interface contract specifies \code{session_id} and other columns on
\code{agent_runs}; only the columns listed above are consumed.  If the
production schema changes, extend via \code{dplyr::any_of()} below.}

\item{jaccard_threshold}{Numeric in the range (0, 1].  Findings on the same file
with Jaccard ≥ this value are treated as the same content.  Default 0.6.}

\item{window_days}{Integer.  Only findings within this many days of the
most recent finding are included.  Default 30.}
}
\value{
A tibble with the \code{roborev_loops} schema.
}
\description{
Groups \code{findings_df} by \code{content_hash} (computed from severity + file +
first 12 words of problem text) and counts "cycles" — pairs of consecutive
occurrences where a matching fix commit appears in \code{agent_runs_df} between
the two reviews.  Findings with different hashes but high Jaccard similarity
on the same file are collapsed to a canonical hash before counting.
}
\details{
Only groups with ≥ 3 cycles are returned.  Tiers:
\itemize{
\item \code{watch}:    3–4 cycles
\item \code{block}:    5–9 cycles
\item \code{escalate}: ≥ 10 cycles
}
}
