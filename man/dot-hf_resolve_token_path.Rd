% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hf_push.R
\name{.hf_resolve_token_path}
\alias{.hf_resolve_token_path}
\title{Resolve the HuggingFace token file path for CI and local use.}
\usage{
.hf_resolve_token_path(token_path, workdir)
}
\arguments{
\item{token_path}{Character.  Preferred path (default \verb{~/.cache/huggingface/token}).}

\item{workdir}{Character.  Directory where a transient token file may be created.}
}
\value{
Character path to the resolved token file.
}
\description{
Checks two sources in order:
\enumerate{
\item If \code{token_path} exists on disk, return it unchanged (local / already-set-up CI).
\item If the \code{HF_TOKEN} environment variable is non-empty, write its value to a
transient file inside \code{workdir} (mode 0600) and return that path.
The transient file's lifetime is bounded by the caller's \code{workdir} cleanup.
}
}
\details{
The token value is written only to a file — it is NEVER stored in an R
variable that outlives this function's local scope, and never printed.
}
\keyword{internal}
