% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_roborev.R
\name{.roborev_raw_to_findings}
\alias{.roborev_raw_to_findings}
\title{Transform roborev_raw into the roborev_findings vignette contract schema}
\usage{
.roborev_raw_to_findings(raw_df)
}
\arguments{
\item{raw_df}{Tibble returned by \code{\link[=scrape_roborev]{scrape_roborev()}}.}
}
\value{
A tibble conforming to the \code{roborev_findings} vignette contract.
}
\description{
Converts the output of \code{\link[=scrape_roborev]{scrape_roborev()}} into the tibble expected by
\code{\link[=plan_roborev_vignette]{plan_roborev_vignette()}}. Column mapping:
}
\details{
\tabular{lll}{
   raw column \tab findings column \tab notes \cr
   \code{job_id} \tab \code{review_id} \tab  \cr
   \code{commit_sha} \tab \code{review_commit} \tab  \cr
   \code{severity} \tab \code{severity} \tab coerced to character \cr
   \code{primary_file} \tab \code{primary_file} \tab  \cr
   \code{problem_text} \tab \code{problem_text} \tab  \cr
   \code{problem_text} \tab \code{summary} \tab first 80 characters \cr
   \code{reviewed_at} \tab \code{found_at} \tab  \cr
   — \tab \code{resolved_at} \tab \code{NA} (not tracked at scrape time) \cr
   — \tab \code{fix_commit} \tab \code{NA} (not tracked at scrape time) \cr
   \code{project} \tab \code{project} \tab  \cr
   \code{agent} \tab \code{agent_id} \tab  \cr
   — \tab \code{finding_id} \tab sequential integer \cr
}


Returns an empty tibble with the correct schema when \code{raw_df} has zero rows,
so downstream targets degrade gracefully when roborev is unavailable.
}
\keyword{internal}
