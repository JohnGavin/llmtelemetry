% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sanitize_session_id.R
\name{.sanitize_session_id_local}
\alias{.sanitize_session_id_local}
\title{Sanitize path-style session_id values.}
\usage{
.sanitize_session_id_local(session_ids, canonical_projects, started_at)
}
\arguments{
\item{session_ids}{Character vector of session IDs (may be mixed path /
non-path).}

\item{canonical_projects}{Character vector (same length) of canonical
project names; use \code{NA_character_} when unknown.}

\item{started_at}{POSIXct vector (same length) of session start times
used as the timestamp component.  When \code{NA}, falls back to the positional
index cast to character.}
}
\value{
A character vector of the same length.  Path-style IDs are replaced
with \code{"sanitized@{project}@{iso8601_utc}@h{hash12}"}.  Non-path IDs pass
through unchanged.
}
\description{
Replaces raw filesystem path session IDs (those starting with \code{"-"} or
containing \code{"/"}) with a deterministic stable identifier that does not
expose private paths.  Non-path IDs are returned unchanged.
}
\keyword{internal}
