% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidential_projects.R
\name{drop_confidential_projects}
\alias{drop_confidential_projects}
\title{Drop rows that contain confidential project names}
\usage{
drop_confidential_projects(df, cols = c("project", "canonical_project"))
}
\arguments{
\item{df}{A data frame to filter.}

\item{cols}{Character vector of column names to check.  Defaults to
\code{c("project", "canonical_project")}.}
}
\value{
The filtered data frame (same class as input).
}
\description{
Removes any row from \code{df} where a project-name column (default:
\code{project} and \code{canonical_project}) case-insensitively matches any entry in
\code{\link[=confidential_project_names]{confidential_project_names()}}, OR whose value starts with \code{"crypto"} or
\code{"mycare"} (catches future sub-projects without requiring a list update).
Columns that are absent from \code{df} are silently skipped.
}
\details{
The function is deliberately conservative: a row is dropped if ANY of the
specified columns matches.  This prevents short-name leaks where only one of
the two columns carries the confidential identifier.
}
\keyword{internal}
