% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_staging.R
\name{read_staging}
\alias{read_staging}
\title{Read all staged events from the llmtelemetry staging directory}
\usage{
read_staging(
  staging_dir = file.path(Sys.getenv("HOME"), ".claude", "logs", "llmtelemetry-staging")
)
}
\arguments{
\item{staging_dir}{Path to the staging directory. Defaults to
\verb{~/.claude/logs/llmtelemetry-staging}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\itemize{
\item \code{ts} (POSIXct UTC): envelope timestamp
\item \code{host} (character): hostname of the emitting machine
\item \code{pid} (character): process ID at emission time
\item \code{payload} (list): parsed JSON payload (one element per row)
}
}
\description{
Reads all \code{.jsonl} files from the staging directory and returns a tibble
with one row per event envelope. Each envelope has a \code{ts} timestamp, \code{host},
\code{pid}, and \code{payload} (a list-column containing the parsed JSON payload).
}
\details{
This function is intentionally read-only and does not modify the staging
directory. Integration of staged events into the rollup pipeline (with
deduplication) is planned for Phase 1E.
}
