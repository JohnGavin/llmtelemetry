% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_costs.R
\name{rollup_costs}
\alias{rollup_costs}
\title{Backfill the v1 costs parquet from cost_by_project_estimated.json}
\usage{
rollup_costs(
  input_path = here::here("inst", "extdata", "cost_by_project_estimated.json"),
  output_path = here::here("inst", "extdata", "telemetry", "v1", "costs.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{input_path}{Path to \code{cost_by_project_estimated.json}. Defaults to the
package's \code{inst/extdata/cost_by_project_estimated.json}.}

\item{output_path}{Where to write \code{costs.parquet}. Defaults to
\code{inst/extdata/telemetry/v1/costs.parquet}.}

\item{now}{Override the \code{valid_from} timestamp. Mainly useful in tests to
produce deterministic output. Defaults to \code{Sys.time()}.}
}
\value{
Invisibly returns a data frame with the transformed rows (v1 schema).
}
\description{
Reads \code{cost_by_project_estimated.json}, transforms columns to the v1 costs
schema, and writes a Parquet file using DuckDB's native COPY ... TO
(compression: zstd). The write is atomic: output is written to a temporary
file in the same directory, then renamed over the final path.
}
\details{
\code{arrow} is not required; DuckDB's built-in Parquet writer is used instead.
}
