% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_git_commits.R
\name{rollup_git_commits}
\alias{rollup_git_commits}
\title{Backfill the v1 git_commits parquet from git_commits_by_project.json}
\usage{
rollup_git_commits(
  input_path = here::here("inst", "extdata", "git_commits_by_project.json"),
  output_path = here::here("inst", "extdata", "telemetry", "v1", "git_commits.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{input_path}{Path to \code{git_commits_by_project.json}. Defaults to the
package's \code{inst/extdata/git_commits_by_project.json}.}

\item{output_path}{Where to write \code{git_commits.parquet}. Defaults to
\code{inst/extdata/telemetry/v1/git_commits.parquet}.}

\item{now}{Override the \code{valid_from} timestamp. Mainly useful in tests to
produce deterministic output. Defaults to \code{Sys.time()}.}
}
\value{
Invisibly returns a data frame with the transformed rows (v1 schema).
}
\description{
Reads \code{git_commits_by_project.json}, transforms columns to the v1
git_commits schema, and writes a Parquet file using DuckDB's native
COPY ... TO (compression: zstd). The write is atomic: output is written
to a temporary file in the same directory, then renamed over the final path.
}
\details{
\code{arrow} is not required; DuckDB's built-in Parquet writer is used instead.

The primary key \code{commit_pk} is a composite of \code{canonical_project|hash}
because short hashes can collide across repositories.
}
