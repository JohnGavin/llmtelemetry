% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup_session_commits.R
\name{rollup_session_commits}
\alias{rollup_session_commits}
\title{Build a session-to-commit link table (v1 schema)}
\usage{
rollup_session_commits(
  sessions_path = here::here("inst", "extdata", "telemetry", "v1", "sessions.parquet"),
  commits_path = here::here("inst", "extdata", "telemetry", "v1", "git_commits.parquet"),
  output_path = here::here("inst", "extdata", "telemetry", "v1",
    "session_commits.parquet"),
  now = Sys.time()
)
}
\arguments{
\item{sessions_path}{Path to \code{sessions.parquet}. Defaults to the package's
\code{inst/extdata/telemetry/v1/sessions.parquet}.}

\item{commits_path}{Path to \code{git_commits.parquet}. Defaults to the package's
\code{inst/extdata/telemetry/v1/git_commits.parquet}.}

\item{output_path}{Where to write \code{session_commits.parquet}. Defaults to
\code{inst/extdata/telemetry/v1/session_commits.parquet}.}

\item{now}{Override the \code{valid_from} timestamp. Mainly useful in tests.
Defaults to \code{Sys.time()}.}
}
\value{
Invisibly returns a data frame with the linked rows (v1 schema).
}
\description{
Joins \code{sessions.parquet} to \code{git_commits.parquet} by matching commits whose
\code{date} falls within \verb{[session.started_at::date, session.ended_at::date]} AND
whose \code{canonical_project} matches the session's \code{canonical_project}.
}
\details{
The result is written atomically to \code{session_commits.parquet} (zstd).
Confidential projects are filtered via \code{drop_confidential_projects()} before
writing so the output can be committed to \verb{inst/extdata/}.
}
\section{Schema}{
\tabular{lll}{
   Column \tab Type \tab Notes \cr
   \code{session_id} \tab character \tab from sessions.parquet \cr
   \code{commit_sha} \tab character \tab short hash from git_commits.hash \cr
   \code{repo} \tab character \tab canonical_project from git_commits \cr
   \code{commit_at} \tab Date \tab git_commits.date (day granularity) \cr
   \code{duration_min} \tab double \tab session.duration_min \cr
   \code{agent} \tab character \tab session.agent (NA when unknown) \cr
}
}

