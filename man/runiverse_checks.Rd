% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runiverse.R
\name{runiverse_checks}
\alias{runiverse_checks}
\title{List per-config build checks for R-Universe packages}
\usage{
runiverse_checks(universe, package = NULL)
}
\arguments{
\item{universe}{Character scalar — the R-Universe subdomain.}

\item{package}{Optional character scalar — restrict to a single package
(passed through to \code{\link[=runiverse_packages]{runiverse_packages()}}).}
}
\value{
A tibble with one row per package x build config: \code{package},
\code{version}, \code{config}, \code{os} (derived from \code{config}), \code{r_version},
\code{check} (ordered factor), \code{build_time} (seconds), \code{artifact}, \code{status}
(the package-level \verb{_status}).
}
\description{
Calls \code{\link[=runiverse_packages]{runiverse_packages()}} and unnests each package's \code{jobs} list-column
(the raw \verb{_jobs} build matrix) into one row per package x build config,
with \code{check} as an ordered factor (\verb{OK < NOTE < WARNING < ERROR < FAIL})
so the worst result for a package sorts to the top of a descending sort.
}
\examples{
\dontrun{
runiverse_checks("johngavin")
runiverse_checks("johngavin", package = "irishbuoys")
}
}
