% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runiverse.R
\name{runiverse_packages}
\alias{runiverse_packages}
\title{List packages published on an R-Universe}
\usage{
runiverse_packages(universe, package = NULL)
}
\arguments{
\item{universe}{Character scalar — the R-Universe subdomain, e.g.
\code{"johngavin"} for \verb{https://johngavin.r-universe.dev}.}

\item{package}{Optional character scalar. When given, fetches only that
package via \verb{/api/packages/\{package\}} instead of the whole universe.}
}
\value{
A tibble with one row per package:
\code{package}, \code{version}, \code{title}, \code{maintainer}, \code{maintainer_email},
\code{owner}, \code{status}, \code{registered}, \code{published}, \code{stars}, \code{downloads},
\code{usedby}, \code{devurl}, \code{pkgdown}, \code{upstream}, and a list-column \code{jobs}
holding each package's raw \verb{_jobs} build-matrix entries — see
\code{\link[=runiverse_checks]{runiverse_checks()}} to unnest them into one row per package x config.
}
\description{
Queries the R-Universe API (\code{GET /api/packages}, or \code{GET /api/packages/{package}} when \code{package} is given) and returns one row
per package summarising build status, ownership, and popularity
metadata. Responses are cached on disk via \code{\link[httr2:req_cache]{httr2::req_cache()}}
(\code{tools::R_user_dir("runiverse", "cache")}), matching the API's own
\code{Cache-Control: max-age=60} header — repeated calls within a short
window reuse the cached response instead of re-hitting the CDN
(CI-minute conservation, see llm#761).
}
\examples{
\dontrun{
runiverse_packages("johngavin")
runiverse_packages("johngavin", package = "irishbuoys")
}
}
