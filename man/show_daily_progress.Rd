% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccusage.R
\name{show_daily_progress}
\alias{show_daily_progress}
\title{Show Today's LLM Usage Progress}
\usage{
show_daily_progress(daily_limit = NULL, token_limit = NULL, cache_dir = NULL)
}
\arguments{
\item{daily_limit}{Daily cost limit in USD. If NULL, reads from environment variable
\code{LLM_DAILY_LIMIT} or defaults to 30.}

\item{token_limit}{Daily token limit. If NULL, reads from environment variable
\code{LLM_DAILY_TOKEN_LIMIT} or defaults to 500000.}

\item{cache_dir}{Directory containing cached JSON files. If NULL, searches standard
package cache locations.}
}
\value{
Invisibly returns a list with elements:
\itemize{
\item \code{cost}: Numeric, current daily cost
\item \code{tokens}: Numeric, current daily token usage
\item \code{cost_pct}: Integer, percentage of daily cost limit used
\item \code{token_pct}: Integer, percentage of daily token limit used
}
}
\description{
Displays progress bars for today's LLM usage against configured daily limits.
Shows both cost and token consumption with visual progress bars and color coding.
Fetches data from cached ccusage files and calculates statistics for the current day.
}
\details{
Display daily usage progress bars
}
\examples{
\dontrun{
# Show today's usage with defaults
show_daily_progress()

# Show today's usage with custom limits
show_daily_progress(daily_limit = 50, token_limit = 750000)
}

}
\seealso{
\itemize{
\item \code{\link[=show_usage_progress]{show_usage_progress()}} for low-level progress bar display
\item \code{\link[=show_weekly_progress]{show_weekly_progress()}} for weekly usage tracking
\item \code{\link[=show_usage_dashboard]{show_usage_dashboard()}} for combined daily/weekly view
\item \code{\link[=load_cached_ccusage]{load_cached_ccusage()}} to load underlying data
}
}
\keyword{internal}
