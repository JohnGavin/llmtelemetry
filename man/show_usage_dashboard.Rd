% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccusage.R
\name{show_usage_dashboard}
\alias{show_usage_dashboard}
\title{Show Complete LLM Usage Dashboard}
\usage{
show_usage_dashboard(
  daily_limit = NULL,
  weekly_limit = NULL,
  token_limit = NULL,
  cache_dir = NULL,
  show_max5 = TRUE
)
}
\arguments{
\item{daily_limit}{Daily cost limit in USD. If NULL, reads from \code{LLM_DAILY_LIMIT}
environment variable or defaults to 30.}

\item{weekly_limit}{Weekly cost limit in USD. If NULL, reads from \code{LLM_WEEKLY_LIMIT}
environment variable or defaults to 120.}

\item{token_limit}{Daily token limit. If NULL, reads from \code{LLM_DAILY_TOKEN_LIMIT}
environment variable or defaults to 500000.}

\item{cache_dir}{Directory containing cached JSON files. If NULL, searches standard
package cache locations.}

\item{show_max5}{Logical whether to show Max5 5-hour block status when the
\code{LLM_PLAN} environment variable is set to "max5". Default: TRUE.}
}
\value{
Invisibly returns a list containing:
\itemize{
\item \code{daily}: List from \code{\link[=show_daily_progress]{show_daily_progress()}}
\item \code{weekly}: List from \code{\link[=show_weekly_progress]{show_weekly_progress()}}
\item \code{max5}: List from \code{\link[=show_max5_block_status]{show_max5_block_status()}} if applicable, otherwise NULL
}
}
\description{
Displays a comprehensive usage dashboard combining daily and weekly progress bars,
token usage, and Max5 block status (if applicable). Includes automated warnings
when approaching cost limits. This is the main entry point for monitoring LLM usage
across different time horizons.
}
\details{
Display combined usage dashboard
}
\examples{
\dontrun{
# Show complete dashboard with default settings
show_usage_dashboard()

# Show dashboard with custom limits
show_usage_dashboard(
  daily_limit = 50,
  weekly_limit = 200,
  token_limit = 750000
)

# Show dashboard without Max5 status
show_usage_dashboard(show_max5 = FALSE)
}

}
\seealso{
\itemize{
\item \code{\link[=show_daily_progress]{show_daily_progress()}} for daily breakdown
\item \code{\link[=show_weekly_progress]{show_weekly_progress()}} for weekly breakdown
\item \code{\link[=show_max5_block_status]{show_max5_block_status()}} for Max5 plan block tracking
\item \code{\link[=show_usage_progress]{show_usage_progress()}} for individual progress bars
}
}
