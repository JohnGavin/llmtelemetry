% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccusage.R
\name{show_usage_progress}
\alias{show_usage_progress}
\title{Display Terminal Progress Bar for LLM Usage}
\usage{
show_usage_progress(
  current,
  limit,
  label = "Usage",
  show_tokens = FALSE,
  tokens_current = NULL,
  tokens_limit = NULL
)
}
\arguments{
\item{current}{Current usage value (typically cost in USD)}

\item{limit}{Maximum limit value (upper bound for the progress bar)}

\item{label}{Character string label for the progress bar (default: "Usage")}

\item{show_tokens}{Logical whether to show token count on a second line (default: FALSE)}

\item{tokens_current}{Current token count (required if show_tokens = TRUE)}

\item{tokens_limit}{Token limit (required if show_tokens = TRUE)}
}
\value{
Invisibly returns the percentage used (numeric 0-100)
}
\description{
Shows a terminal progress bar with current usage vs limits. The bar includes
a visual indicator using Unicode box characters, percentage completion, and
formatted values. Colors change based on usage thresholds (green < 75\%, yellow
75-89\%, red >= 90\%).
}
\details{
Display progress bar for LLM usage
}
\examples{
\dontrun{
# Show basic cost progress bar
show_usage_progress(15, 30, label = "Daily Cost")

# Show cost and token usage
show_usage_progress(
  current = 25,
  limit = 30,
  label = "Daily Usage",
  show_tokens = TRUE,
  tokens_current = 450000,
  tokens_limit = 500000
)
}

}
\seealso{
\itemize{
\item \code{\link[=show_daily_progress]{show_daily_progress()}} for today's complete usage summary
\item \code{\link[=show_weekly_progress]{show_weekly_progress()}} for weekly usage tracking
\item \code{\link[=show_usage_dashboard]{show_usage_dashboard()}} for combined daily/weekly dashboard
}
}
\keyword{internal}
