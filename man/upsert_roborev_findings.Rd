% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_roborev.R
\name{upsert_roborev_findings}
\alias{upsert_roborev_findings}
\title{Upsert roborev findings into unified.duckdb}
\usage{
upsert_roborev_findings(findings_df, db_path)
}
\arguments{
\item{findings_df}{Tibble returned by \code{\link[=scrape_roborev]{scrape_roborev()}}.}

\item{db_path}{Character path to the DuckDB database file.}
}
\value{
A tibble with summary columns \code{reviews_seen}, \code{new_findings},
\code{new_reviews}.
}
\description{
Opens the DuckDB at \code{db_path}, inserts new review records into
\code{roborev_reviews} and new finding records into \code{roborev_findings}
(idempotent — duplicates by \verb{(job_id, content_hash)} are skipped),
and logs the run in \code{roborev_snapshot_log}.
}
